This is a resume parser that extracts information from pdf or docx file and creates json structured data for easy addition into databases. Using NLP and search patters, the parser recognizes where vital information is located in a file and extracts.

The parser has API included
